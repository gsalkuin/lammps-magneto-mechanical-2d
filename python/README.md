# Python package to generate LAMMPS data files and examples


## Installation

- (Optional) Create and activate Conda environment: 
```bash
conda env create -f environment.yml
conda activate lmm2d
```

- Install with pip:
```bash
pip install .
```